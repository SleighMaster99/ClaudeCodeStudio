<workflow>
  <critical>🛑 USER INVOCATION REQUIRED: dev-story는 사용자가 에이전트 메뉴 또는 직접 명령으로 명시적으로 호출해야 합니다. create-story, create-epics-and-stories 등 다른 워크플로우에서 자동 체이닝으로 도달한 경우 즉시 중단하고 사용자에게 제어를 반환하세요.</critical>
  <critical>The workflow execution engine is governed by: {project-root}/_bmad/core/tasks/workflow.xml</critical>
  <critical>You MUST have already loaded and processed: {installed_path}/workflow.yaml</critical>
  <critical>Communicate all responses in {communication_language} and language MUST be tailored to {user_skill_level}</critical>
  <critical>Generate all documents in {document_output_language}</critical>
  <critical>Only modify the story file in these areas: Tasks/Subtasks checkboxes, Dev Agent Record (Debug Log, Completion Notes), File List,
    Change Log, and Status</critical>
  <critical>Execute ALL steps in exact order; do NOT skip steps</critical>
  <critical>Absolutely DO NOT stop because of "milestones", "significant progress", or "session boundaries". Continue in a single execution
    until the story is COMPLETE (all ACs satisfied and all tasks/subtasks checked) UNLESS a HALT condition is triggered or the USER gives
    other instruction.</critical>
  <critical>Do NOT schedule a "next session" or request review pauses unless a HALT condition applies. Only Step 6 decides completion.</critical>
  <critical>User skill level ({user_skill_level}) affects conversation style ONLY, not code updates.</critical>

  <step n="1" goal="Find next ready story and load it" tag="sprint-status">
    <check if="{{story_path}} is provided">
      <action>Use {{story_path}} directly</action>
      <action>Read COMPLETE story file</action>
      <action>Extract story_key from filename or metadata</action>
      <goto anchor="task_check" />
    </check>

    <!-- Sprint-based story discovery -->
    <check if="{{sprint_status}} file exists">
      <critical>MUST read COMPLETE sprint-status.yaml file from start to end to preserve order</critical>
      <action>Load the FULL file: {{sprint_status}}</action>
      <action>Read ALL lines from beginning to end - do not skip any content</action>
      <action>Parse the development_status section completely to understand story order</action>

      <action>Find the FIRST story (by reading in order from top to bottom) where:
        - Key matches pattern: number-number-name (e.g., "1-2-user-auth")
        - NOT an epic key (epic-X) or retrospective (epic-X-retrospective)
        - Status value equals "ready-for-dev"
      </action>

      <check if="no ready-for-dev or in-progress story found">
        <output>📋 No ready-for-dev stories found in sprint-status.yaml

          **Current Sprint Status:** {{sprint_status_summary}}

          **What would you like to do?**
          1. Run `create-story` to create next story from epics with comprehensive context
          2. Run `*validate-create-story` to improve existing stories before development (recommended quality check)
          3. Specify a particular story file to develop (provide full path)
          4. Check {{sprint_status}} file to see current sprint status

          💡 **Tip:** Stories in `ready-for-dev` may not have been validated. Consider running `validate-create-story` first for a quality
          check.
        </output>
        <ask>Choose option [1], [2], [3], or [4], or specify story file path:</ask>

        <check if="user chooses '1'">
          <action>HALT - Run create-story to create next story</action>
        </check>

        <check if="user chooses '2'">
          <action>HALT - Run validate-create-story to improve existing stories</action>
        </check>

        <check if="user chooses '3'">
          <ask>Provide the story file path to develop:</ask>
          <action>Store user-provided story path as {{story_path}}</action>
          <goto anchor="task_check" />
        </check>

        <check if="user chooses '4'">
          <output>Loading {{sprint_status}} for detailed status review...</output>
          <action>Display detailed sprint status analysis</action>
          <action>HALT - User can review sprint status and provide story path</action>
        </check>

        <check if="user provides story file path">
          <action>Store user-provided story path as {{story_path}}</action>
          <goto anchor="task_check" />
        </check>
      </check>
    </check>

    <!-- Non-sprint story discovery -->
    <check if="{{sprint_status}} file does NOT exist">
      <action>Search {story_dir} for stories directly</action>
      <action>Find stories with "ready-for-dev" status in files</action>
      <action>Look for story files matching pattern: *-*-*.md</action>
      <action>Read each candidate story file to check Status section</action>

      <check if="no ready-for-dev stories found in story files">
        <output>📋 No ready-for-dev stories found

          **Available Options:**
          1. Run `create-story` to create next story from epics with comprehensive context
          2. Run `*validate-create-story` to improve existing stories
          3. Specify which story to develop
        </output>
        <ask>What would you like to do? Choose option [1], [2], or [3]:</ask>

        <check if="user chooses '1'">
          <action>HALT - Run create-story to create next story</action>
        </check>

        <check if="user chooses '2'">
          <action>HALT - Run validate-create-story to improve existing stories</action>
        </check>

        <check if="user chooses '3'">
          <ask>It's unclear what story you want developed. Please provide the full path to the story file:</ask>
          <action>Store user-provided story path as {{story_path}}</action>
          <action>Continue with provided story file</action>
        </check>
      </check>

      <check if="ready-for-dev story found in files">
        <action>Use discovered story file and extract story_key</action>
      </check>
    </check>

    <action>Store the found story_key (e.g., "1-2-user-authentication") for later status updates</action>
    <action>Find matching story file in {story_dir} using story_key pattern: {{story_key}}.md</action>
    <action>Read COMPLETE story file from discovered path</action>

    <anchor id="task_check" />

    <action>Parse sections: Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes, Dev Agent Record, File List, Change Log, Status</action>

    <action>Load comprehensive context from story file's Dev Notes section</action>
    <action>Extract developer guidance from Dev Notes: architecture requirements, previous learnings, technical specifications</action>
    <action>Use enhanced story context to inform implementation decisions and approaches</action>

    <action>Identify first incomplete task (unchecked [ ]) in Tasks/Subtasks</action>

    <action if="no incomplete tasks">
      <goto step="6">Completion sequence</goto>
    </action>
    <action if="story file inaccessible">HALT: "Cannot develop story without access to story file"</action>
    <action if="incomplete task or subtask requirements ambiguous">ASK user to clarify or HALT</action>
  </step>

  <step n="2" goal="Load project context and story information">
    <critical>Load all available context to inform implementation</critical>

    <action>Load {project_context} for coding standards and project-wide patterns (if exists)</action>
    <action>Load ALL coding rule documents matched by {coding_conventions} (every doc under docs/CodingRule/) for project coding rules (if any exist)</action>
    <action>Parse sections: Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes, Dev Agent Record, File List, Change Log, Status</action>
    <action>Load comprehensive context from story file's Dev Notes section</action>
    <action>Extract developer guidance from Dev Notes: architecture requirements, previous learnings, technical specifications</action>
    <action>Use enhanced story context to inform implementation decisions and approaches</action>
    <output>✅ **Context Loaded**
      Story and project context available for implementation
    </output>
  </step>

  <step n="3" goal="Detect review continuation and extract review context">
    <critical>Determine if this is a fresh start or continuation after code review</critical>

    <action>Check if "Senior Developer Review (AI)" section exists in the story file</action>
    <action>Check if "Review Follow-ups (AI)" subsection exists under Tasks/Subtasks</action>

    <check if="Senior Developer Review section exists">
      <action>Set review_continuation = true</action>
      <action>Extract from "Senior Developer Review (AI)" section:
        - Review outcome (Approve/Changes Requested/Blocked)
        - Review date
        - Total action items with checkboxes (count checked vs unchecked)
        - Severity breakdown (High/Med/Low counts)
      </action>
      <action>Count unchecked [ ] review follow-up tasks in "Review Follow-ups (AI)" subsection</action>
      <action>Store list of unchecked review items as {{pending_review_items}}</action>

      <output>⏯️ **Resuming Story After Code Review** ({{review_date}})

        **Review Outcome:** {{review_outcome}}
        **Action Items:** {{unchecked_review_count}} remaining to address
        **Priorities:** {{high_count}} High, {{med_count}} Medium, {{low_count}} Low

        **Strategy:** Will prioritize review follow-up tasks (marked [AI-Review]) before continuing with regular tasks.
      </output>
    </check>

    <check if="Senior Developer Review section does NOT exist">
      <action>Set review_continuation = false</action>
      <action>Set {{pending_review_items}} = empty</action>

      <output>🚀 **Starting Fresh Implementation**

        Story: {{story_key}}
        Story Status: {{current_status}}
        First incomplete task: {{first_task_description}}
      </output>
    </check>
  </step>

  <step n="4" goal="Mark story in-progress" tag="sprint-status">
    <check if="{{sprint_status}} file exists">
      <action>Load the FULL file: {{sprint_status}}</action>
      <action>Read all development_status entries to find {{story_key}}</action>
      <action>Get current status value for development_status[{{story_key}}]</action>

      <check if="current status == 'ready-for-dev' OR review_continuation == true">
        <action>Update the story in the sprint status report to = "in-progress"</action>
        <output>🚀 Starting work on story {{story_key}}
          Status updated: ready-for-dev → in-progress
        </output>
      </check>

      <check if="current status == 'in-progress'">
        <output>⏯️ Resuming work on story {{story_key}}
          Story is already marked in-progress
        </output>
      </check>

      <check if="current status is neither ready-for-dev nor in-progress">
        <output>⚠️ Unexpected story status: {{current_status}}
          Expected ready-for-dev or in-progress. Continuing anyway...
        </output>
      </check>

      <action>Store {{current_sprint_status}} for later use</action>
    </check>

    <check if="{{sprint_status}} file does NOT exist">
      <output>ℹ️ No sprint status file exists - story progress will be tracked in story file only</output>
      <action>Set {{current_sprint_status}} = "no-sprint-tracking"</action>
    </check>
  </step>

  <step n="4.5" goal="Git 브랜치 설정">
    <critical>🌿 GIT BRANCH SETUP - Ensure proper branch for development!</critical>

    <action>스토리 파일에서 GitHub Tracking 섹션 읽기</action>
    <action>{{issue_number}}, {{branch_name}}, Story-Type 필드에서 {{story_type}} 추출</action>

    <check if="{{issue_number}}가 비어있거나 GitHub Tracking 섹션 없음">
      <output>⚠️ 이 스토리에 연결된 GitHub 이슈가 없습니다.

        **옵션:**
        1. SM에게 이슈 생성 요청 (create-story 재실행)
        2. SM에게 *link-issue 요청 (기존 이슈 연결)
        3. 이슈 없이 수동으로 진행
      </output>
      <ask>이슈 없이 진행하시겠습니까? [y/n]</ask>
      <check if="user says no">
        <action>HALT - 이슈 연결 후 재시작 필요</action>
      </check>
      <check if="user says yes">
        <action>{{story_type}} 기본값 설정: "feat"</action>
        <action>{{branch_name}} 생성: feat/{{story_key}}</action>
      </check>
    </check>

    <check if="git repository detected (run: git rev-parse --git-dir)">
      <action>현재 브랜치 확인: git branch --show-current</action>

      <check if="현재 브랜치가 main 또는 master">
        <action>새 브랜치 생성 및 체크아웃: git checkout -b {{branch_name}}</action>
        <output>🌿 브랜치 생성됨: {{branch_name}}</output>
      </check>

      <check if="{{branch_name}} 브랜치가 이미 존재 (git branch --list)">
        <action>기존 브랜치로 체크아웃: git checkout {{branch_name}}</action>
        <output>🌿 기존 브랜치로 체크아웃: {{branch_name}} (create-story에서 생성된 브랜치)</output>
      </check>

      <check if="이미 {{branch_name}} 브랜치에 있음">
        <output>✅ 이미 올바른 브랜치에 있음: {{branch_name}} (create-story에서 체크아웃됨)</output>
      </check>

      <action>스토리 파일의 GitHub Tracking > Branch 필드에 실제 브랜치명 기록</action>
    </check>

    <check if="git repository NOT detected">
      <output>ℹ️ Git 저장소가 감지되지 않음 - 브랜치 관리 건너뜀</output>
    </check>
  </step>

  <step n="4.8" goal="Verify unverified technical claims before implementation">
    <critical>🔍 VERIFY BEFORE CODING — [unverified] items from create-story MUST be confirmed against reality</critical>

    <action>Scan story Dev Notes for all items tagged [unverified]</action>

    <check if="[unverified] items found">
      <action>For EACH [unverified] item:
        1. Execute the verification method specified in the story (e.g., run --help, check type definitions, ls/read files)
        2. Record verification result in Dev Agent Record:
           - "✅ Verified: [claim] — confirmed by [method]: [evidence]"
           - "❌ Verification failed: [claim] — actual: [reality]"
        3. If verified: update the story tag from [unverified] to [verified]
        4. If verification fails: STOP, correct the story spec to match reality, document the correction in Dev Agent Record, then proceed with corrected spec
      </action>
      <output>🔍 **Technical Verification Results:**
        - Verified: {{verified_count}} items
        - Corrected: {{corrected_count}} items (story specs updated)
      </output>
    </check>

    <check if="no [unverified] items found">
      <action>Spot-check 2-3 key technical claims in Dev Notes (CLI commands, API signatures, file paths) even if tagged [verified]</action>
      <action>If any spot-check fails: HALT, correct story, document in Dev Agent Record</action>
    </check>
  </step>

  <step n="5" goal="Implement task following red-green-refactor cycle">
    <critical>FOLLOW THE STORY FILE TASKS/SUBTASKS SEQUENCE EXACTLY AS WRITTEN - NO DEVIATION</critical>
    <critical>UNIT = each subtask. If a task has NO subtasks, the task itself is the unit. The Step 5→6→7→8 loop and git commit operate PER UNIT.</critical>

    <action>Review the current unit from the story file - this is your authoritative implementation guide</action>
    <action>Plan implementation following red-green-refactor cycle</action>

    <!-- RED PHASE -->
    <action>Write FAILING tests first for the task/subtask functionality</action>
    <action>Confirm tests fail before implementation - this validates test correctness</action>

    <!-- GREEN PHASE -->
    <action>Implement MINIMAL code to make tests pass</action>
    <action>Run tests to confirm they now pass</action>
    <action>Handle error conditions and edge cases as specified in task/subtask</action>

    <!-- REFACTOR PHASE -->
    <action>Improve code structure while keeping tests green</action>
    <action>Ensure code follows architecture patterns and coding standards from Dev Notes</action>

    <action>Document technical approach and decisions in Dev Agent Record → Implementation Plan</action>

    <action if="new dependencies required beyond story specifications">HALT: "Additional dependencies need user approval"</action>
    <action if="3 consecutive implementation failures occur">HALT and request guidance</action>
    <action if="required configuration is missing">HALT: "Cannot proceed without necessary configuration files"</action>

    <critical>NEVER implement anything not mapped to a specific task/subtask in the story file</critical>
    <critical>NEVER proceed to next unit until current unit is complete AND tests pass</critical>
    <critical>Execute the Step 5→6→7→8 loop autonomously for each unit without asking for user confirmation — but ALWAYS complete Step 8 (including git commit) before proceeding to the next unit</critical>
    <critical>Do NOT propose to pause for review until Step 9 completion gates are satisfied</critical>
  </step>

  <step n="6" goal="Author comprehensive tests">
    <action>Create unit tests for business logic and core functionality introduced/changed by the task</action>
    <action>Add integration tests for component interactions specified in story requirements</action>
    <action>Include end-to-end tests for critical user flows when story requirements demand them</action>
    <action>Cover edge cases and error handling scenarios identified in story Dev Notes</action>
  </step>

  <step n="7" goal="Run validations and tests">
    <action>Determine how to run tests for this repo (infer test framework from project structure)</action>
    <action>Run all existing tests to ensure no regressions</action>
    <action>Run the new tests to verify implementation correctness</action>
    <action>Run linting and code quality checks if configured in project</action>
    <action>Validate implementation meets ALL story acceptance criteria; enforce quantitative thresholds explicitly</action>
    <action if="regression tests fail">STOP and fix before continuing - identify breaking changes immediately</action>
    <action if="new tests fail">STOP and fix before continuing - ensure implementation correctness</action>
  </step>

  <step n="8" goal="Validate and mark unit complete ONLY when fully done">
    <critical>NEVER mark a unit complete unless ALL conditions are met - NO LYING OR CHEATING</critical>
    <critical>🔴 MANDATORY COMMIT RULE: 각 unit (subtask 또는 subtask 없는 task) 완료 후 반드시 개별 커밋 실행 - 건너뛰기 절대 금지</critical>
    <critical>🔴 여러 unit 을 연속 처리하더라도, 각 unit 마다 반드시 개별 커밋 수행</critical>
    <critical>🔴 커밋 없이 다음 unit 으로 goto 진행 시 HALT - 커밋 먼저 완료해야 함</critical>

    <!-- VALIDATION GATES -->
    <action>Verify ALL tests for this task/subtask ACTUALLY EXIST and PASS 100%</action>
    <action>Confirm implementation matches EXACTLY what the task/subtask specifies - no extra features</action>
    <action>Validate that ALL acceptance criteria related to this task are satisfied</action>
    <action>Run full test suite to ensure NO regressions introduced</action>

    <!-- REVIEW FOLLOW-UP HANDLING -->
    <check if="task is review follow-up (has [AI-Review] prefix)">
      <action>Extract review item details (severity, description, related AC/file)</action>
      <action>Add to resolution tracking list: {{resolved_review_items}}</action>

      <!-- Mark task in Review Follow-ups section -->
      <action>Mark task checkbox [x] in "Tasks/Subtasks → Review Follow-ups (AI)" section</action>

      <!-- CRITICAL: Also mark corresponding action item in review section -->
      <action>Find matching action item in "Senior Developer Review (AI) → Action Items" section by matching description</action>
      <action>Mark that action item checkbox [x] as resolved</action>

      <action>Add to Dev Agent Record → Completion Notes: "✅ Resolved review finding [{{severity}}]: {{description}}"</action>
    </check>

    <!-- ONLY MARK COMPLETE IF ALL VALIDATION PASS -->
    <check if="ALL validation gates pass AND tests ACTUALLY exist and pass">
      <action>ONLY THEN mark the current unit's checkbox with [x]. If the unit is the last remaining subtask of its parent task, also mark the parent task's checkbox [x].</action>
      <action>Update File List section with ALL new, modified, or deleted files (paths relative to repo root)</action>
      <action>Add completion notes to Dev Agent Record summarizing what was ACTUALLY implemented and tested</action>

      <!-- GIT COMMIT FOR COMPLETED TASK - MANDATORY -->
      <critical>Git commit is NOT a pause — it is a MANDATORY part of each unit completion cycle. Do NOT batch commits across multiple units (subtasks or tasks).</critical>
      <critical id="mandatory-task-commit">
        <check if="git repository detected">
          <action>🔴 [MANDATORY] 변경된 파일 스테이징: git add {{modified_files_for_this_unit}}</action>
          <action>🔴 [MANDATORY] unit 커밋 생성 - 반드시 실행:
            git commit -m "{{story_type}}(story-{{epic_num}}.{{story_num}}): Task {{task_id}} - {{unit_brief_description}} (#{{issue_number}})"
            (task_id = subtask 식별자 예: "13.7" / subtask 없는 task 면 task 식별자 예: "13")
          </action>
          <output>📝 커밋 완료: Task {{task_id}}</output>
          <action>커밋 실행 확인 - 커밋 해시가 생성되어야 다음 unit 진행 가능</action>
        </check>
        <check if="git repository NOT detected">
          <output>ℹ️ Git 저장소 아님 - 커밋 건너뜀</output>
        </check>
      </critical>
    </check>

    <check if="ANY validation fails">
      <action>DO NOT mark unit complete - fix issues first</action>
      <action>HALT if unable to fix validation failures</action>
    </check>

    <check if="review_continuation == true and {{resolved_review_items}} is not empty">
      <action>Count total resolved review items in this session</action>
      <action>Add Change Log entry: "Addressed code review findings - {{resolved_count}} items resolved (Date: {{date}})"</action>
    </check>

    <action>Save the story file</action>
    <action>Determine if more incomplete units remain</action>
    <action if="more units remain">
      <goto step="5">Next unit</goto>
    </action>
    <action if="no units remain">
      <goto step="9">Completion</goto>
    </action>
  </step>

  <step n="9" goal="Story completion and mark for review" tag="sprint-status">
    <action>Verify ALL tasks and subtasks are marked [x] (re-scan the story document now)</action>
    <action>Run the full regression suite (do not skip)</action>
    <action>Confirm File List includes every changed file</action>
    <action>Execute enhanced definition-of-done validation</action>
    <action>Update the story Status to: "review"</action>

    <!-- Enhanced Definition of Done Validation -->
    <action>Validate definition-of-done checklist with essential requirements:
      - All tasks/subtasks marked complete with [x]
      - Implementation satisfies every Acceptance Criterion
      - Unit tests for core functionality added/updated
      - Integration tests for component interactions added when required
      - End-to-end tests for critical flows added when story demands them
      - All tests pass (no regressions, new tests successful)
      - Code quality checks pass (linting, static analysis if configured)
      - File List includes every new/modified/deleted file (relative paths)
      - Dev Agent Record contains implementation notes
      - Change Log includes summary of changes
      - Only permitted story sections were modified
    </action>

    <!-- Mark story ready for review - sprint status conditional -->
    <check if="{sprint_status} file exists AND {{current_sprint_status}} != 'no-sprint-tracking'">
      <action>Load the FULL file: {sprint_status}</action>
      <action>Find development_status key matching {{story_key}}</action>
      <action>Verify current status is "in-progress" (expected previous state)</action>
      <action>Update development_status[{{story_key}}] = "review"</action>
      <action>Save file, preserving ALL comments and structure including STATUS DEFINITIONS</action>
      <output>✅ Story status updated to "review" in sprint-status.yaml</output>
    </check>

    <check if="{sprint_status} file does NOT exist OR {{current_sprint_status}} == 'no-sprint-tracking'">
      <output>ℹ️ Story status updated to "review" in story file (no sprint tracking configured)</output>
    </check>

    <check if="story key not found in sprint status">
      <output>⚠️ Story file updated, but sprint-status update failed: {{story_key}} not found

        Story status is set to "review" in file, but sprint-status.yaml may be out of sync.
      </output>
    </check>

    <!-- Final validation gates -->
    <action if="any task is incomplete">HALT - Complete remaining tasks before marking ready for review</action>
    <action if="regression failures exist">HALT - Fix regression issues before completing</action>
    <action if="File List is incomplete">HALT - Update File List with all changed files</action>
    <action if="definition-of-done validation fails">HALT - Address DoD failures before completing</action>
  </step>

  <step n="9.5" goal="Git Push 및 Pull Request 생성">
    <critical>🔀 CREATE PULL REQUEST - Final step before code review!</critical>

    <check if="git repository detected AND {{issue_number}} exists">
      <action>최종 커밋 (스토리 완료):
        git add .
        git commit -m "docs(story-{{epic_num}}.{{story_num}}): Story ready for review (#{{issue_number}})"
      </action>

      <action>원격 저장소에 푸시:
        git push -u origin {{branch_name}}
      </action>

      <check if="push 성공">
        <action>PR 생성:
          gh pr create \
            --title "{{story_type}}: Story {{epic_num}}.{{story_num}} - {{story_title}}" \
            --body "Closes #{{issue_number}}

## Summary
{{story_statement}}

## Changes
{{file_list_summary}}

## Acceptance Criteria
{{ac_checklist}}

## Story File
\`{{story_file_path}}\`"
        </action>

        <check if="PR 생성 성공">
          <action>PR 번호와 URL 추출</action>
          <action>스토리 파일의 GitHub Tracking 섹션 업데이트:
            - PR: #{{pr_number}}
            - PR URL: {{pr_url}}
          </action>
          <output>🔗 Pull Request 생성됨: #{{pr_number}}
            URL: {{pr_url}}
          </output>
        </check>

        <check if="PR 생성 실패">
          <output>⚠️ PR 생성 실패: {{error_message}}
            브랜치는 푸시되었습니다. 수동으로 PR을 생성해주세요.
            브랜치: {{branch_name}}
          </output>
        </check>
      </check>

      <check if="push 실패">
        <output>⚠️ Git push 실패: {{error_message}}
          로컬 변경사항은 저장되었습니다. 네트워크 연결을 확인하고 수동으로 push해주세요.
        </output>
      </check>
    </check>

    <check if="git repository NOT detected OR {{issue_number}} empty">
      <output>ℹ️ GitHub 연동 없이 스토리 완료 - PR 생성 건너뜀</output>
    </check>
  </step>

  <step n="10" goal="Completion communication and user support">
    <action>Execute the enhanced definition-of-done checklist using the validation framework</action>
    <action>Prepare a concise summary in Dev Agent Record → Completion Notes</action>

    <action>Communicate to {user_name} that story implementation is complete and ready for review</action>
    <action>Summarize key accomplishments: story ID, story key, title, key changes made, tests added, files modified</action>
    <action>Provide the story file path and current status (now "review")</action>

    <critical>🎯 수동 테스트 범위 원칙 (반드시 준수):
      - 수동 테스트는 본 스토리 목표 (AC 직접 매핑 새 기능) 의 검증에만 한정한다
      - AC 가 N 개면 보통 N 개 또는 그 이하의 최소 검증 세트를 제시한다 (자산 부담 적은 항목 우선)
      - 회귀 시나리오 (기존 동작 보존 검증) 는 수동 테스트 목록에 자동 포함하지 않는다
      - 스토리 문서에 "회귀 시나리오" 표나 Task 가 명시되어 있어도 동일 — 수동 테스트 안내 시 자동 포함 금지
      - 회귀 위험은 "빌드 통과 + 코드 리뷰 + 자동 테스트 / 정적 검사로 커버됨" 을 보고에 명시한다
      - 사용자가 명시적으로 회귀 시나리오 검증을 요청한 경우에만 추가한다
      - Why: 새 기능 수동 검증과 회귀 보존 검증은 별개 관심사이며, 회귀를 수동 테스트로 떠넘기면 자산 부담이 커지고 본 작업 검증 효율이 떨어진다
    </critical>

    <action>스토리의 Acceptance Criteria 를 기준으로 수동 검증 항목 세트를 구성:
      - 각 AC 마다 사용자가 직접 확인할 수 있는 최소 단위의 검증 절차를 1 개 (또는 그 이하) 작성
      - 자산 부담이 적은 (실행 시간 짧고 준비물 적은) 시나리오 우선
      - 스토리에 "회귀 시나리오" / "기존 동작 보존 테스트" / "Regression Tests" 류 섹션 / 표 / Task 가 존재하더라도 이 목록에 포함하지 않는다
      - 회귀 위험 커버리지는 본 보고서의 별도 문단에 "빌드 통과 + 코드 리뷰 + 자동 테스트 / 정적 검사로 커버됨" 으로 명시
    </action>

    <action>수동 테스트 안내 출력 형식 예시:
      ## ✅ 사용자 수동 검증 항목 (본 스토리 AC 한정)

      - [ ] AC1: {{ac1 요약}} — {{검증 절차 한 줄}}
      - [ ] AC2: {{ac2 요약}} — {{검증 절차 한 줄}}
      - ...

      ## 🛡️ 회귀 위험 커버리지
      - 회귀 시나리오는 본 수동 테스트 목록에서 제외함 (별개 관심사)
      - 회귀 위험은 빌드 통과 + 코드 리뷰 + 자동 테스트 / 정적 검사로 커버됨
      - 회귀 시나리오 수동 검증이 필요하면 명시적으로 요청해 주세요
    </action>

    <action>Based on {user_skill_level}, ask if user needs any explanations about:
      - What was implemented and how it works
      - Why certain technical decisions were made
      - Any patterns, libraries, or approaches used
      - Anything else they'd like clarified
    </action>

    <check if="user asks for explanations">
      <action>Provide clear, contextual explanations tailored to {user_skill_level}</action>
      <action>Use examples and references to specific code when helpful</action>
    </check>

    <action>Once explanations are complete (or user indicates no questions), suggest logical next steps</action>
    <action>Recommended next steps (flexible based on project setup):
      - Review the implemented story and test the changes
      - Verify all acceptance criteria are met
      - Ensure deployment readiness if applicable
      - Run `code-review` workflow for peer review
      - Optional: If Test Architect module installed, run `/bmad:tea:automate` to expand guardrail tests
    </action>

    <output>💡 **Tip:** For best results, run `code-review` using a **different** LLM than the one that implemented this story.</output>
    <check if="{sprint_status} file exists">
      <action>Suggest checking {sprint_status} to see project progress</action>
    </check>
    <action>Remain flexible - allow user to choose their own path or ask for other assistance</action>
  </step>

</workflow>
